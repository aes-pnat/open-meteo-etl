# Databricks notebook source
# TODO: Transform Bronze → Silver
# 1. Create Silver table if not exists
# 2. Run transform (parse JSON, explode arrays, cast types, MERGE)
# 3. Sanity-check date range and row count per location
